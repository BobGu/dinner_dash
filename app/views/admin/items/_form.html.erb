<%= form_for(@item) do |f| %>
	<% if @item.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@item.errors.count, "error") %> prohibited this pie from being saved:</h2>

			<ul>
				<% @item.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group">
		<%= f.label :title %>
		<%= f.text_field :title, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :description %>
		<%= f.text_field :description, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :price_slice %>
		<%= f.text_field :price_slice, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :price_pie %>
		<%= f.text_field :price_pie, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :category_names %>
		<%= f.text_field :category_names, class: "form-control" %>
	</div>

	<div class="actions">
		<%= f.submit value="Save Changes", class: "btn btn-primary" %>
	</div>
<% end %>
