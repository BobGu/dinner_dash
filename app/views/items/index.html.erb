<h1>Our Dangerously Delicious Pies</h1>
<% @items.each do |item| %>
	<div class='pie'>
		<h2 class='pie_title'><strong><%= item.title %></strong><br></h2>
		<p class='pie_description'><%= item.description %><br></p>
		<p class='pie_price'>
			<%= item.price_slice %> /
			<%= item.price_pie %>
	  </p>
	  <%= link_to 'Add Key Lime', edit_cart_path(item) %>

	</div>
<% end %>
